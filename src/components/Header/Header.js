import './Header.scss';
import { Link, withRouter } from "react-router-dom";

function Header() {
    return (
        <header className='header'>
            

        </header>
    );
}

export default Header;